import subprocess
import sqlite3
import os
import time

# ================= CẤU HÌNH CỨNG =================
MY_COOKIE = "_|WARNING:-DO-NOT-SHARE-THIS.--Sharing-this-will-allow-someone-to-log-in-as-you-and-to-steal-your-ROBUX-and-items.|_CAEaAhADIhwKBGR1aWQSFDEwNTAxOTk1ODgxOTczMjgzNDc1KAM.K_gMy3tCQ0KZxZlwirOxqpIwxATitqRN4aaka_b7n0b0qvn6Af-ZH2oyOoNXj3FoGe2u9_RDiUOD24ipW4SGBuYayFwIt5OzfiFAU8HzzIsVDz1GyfKRnn-X3vijUVqM9DQrNQEAONL4jtCX7cgZfNywQOrYxKqcQRg1eyy45g7lhz7flC7SVnhTMB7U9vcanh9CI4ixsHKvGZYtIKzEsvR5f0rjO9evtqURbortrkXq80pFAeZU2UVhnW4R8U85eO193zk_7pv8JaKcz_NklQhO2xxCmMipRstDe-M_fYhGtFltl4olXVagx7SYoUgX-UqYZgaRmGbmxt9LQC-uZJ_-yJZalxHNQGstudubVpcWQFUbPUUjZrnZSHXo2NL8nZORTNyosZJMShdK553t2eLBZv8ocnoCbpRUktt9z9Hsg8rx7G2SWF3ZpMdQFmDIIFymM5YQugkndXdzwqjtaRUYoMZ8XL8mjqPexp6W-rIMUUpMkhfFif9sgQVWP0UtmVAMcWgehGhMjnLxnWKm2i9NRByzjjfBO6mjqAdC3d2lfCCaa2ICWF5j3gWDBRsCuLKpg7uUYy-Vp_Bp00rNN4sGsSZ8yqA79mpmoHHFifwAycURfjtortETJH9nzYSYGwexcA"
PLACE_ID = "8737602449" 
PACKAGE_NAME = "com.roblox.clieng" 

# Đường dẫn database và file tạm
REAL_DB = f"/data/data/{PACKAGE_NAME}/app_webview/Default/Cookies"
TEMP_DB = "/data/local/tmp/temp_roblox.db" 
# =================================================

def run_tsu(command):
    """Sử dụng tsu thay vì su để chạy lệnh root ổn định hơn"""
    # Nếu máy không có tsu, nó sẽ tự động dùng su để thay thế
    try:
        return subprocess.run(['tsu', '-c', command], capture_output=True, text=True)
    except FileNotFoundError:
        return subprocess.run(['su', '-c', command], capture_output=True, text=True)

def get_uid():
    """Lấy UID của ứng dụng để trả quyền sở hữu (Logic improvev2.py)"""
    res = run_tsu(f"stat -c '%u:%g' /data/data/{PACKAGE_NAME}")
    return res.stdout.strip() if res.returncode == 0 else "10100:10100"

def execute_process():
    print(f"\033[1;96m[ Rokid Manager ] - Khởi chạy với tsu cho: {PACKAGE_NAME}\033[0m")

    # BƯỚC 1: Tắt Roblox và dọn dẹp file nhật ký SQLite
    # Nếu không xóa -wal, database sẽ bị khóa ở trạng thái Read-only
    run_tsu(f"am force-stop {PACKAGE_NAME}")
    run_tsu(f"rm -f {REAL_DB}-wal")
    run_tsu(f"rm -f {REAL_DB}-shm")
    time.sleep(1)

    # BƯỚC 2: COPY FILE RA VÀ CẤP QUYỀN TRUY CẬP TỐI ĐA
    print("📂 Đang trích xuất file database...")
    run_tsu(f"cp {REAL_DB} {TEMP_DB}")
    run_tsu(f"chmod 777 {TEMP_DB}") # Đảm bảo Python có quyền ghi vào file tạm

    # BƯỚC 3: SỬA FILE BẰNG SQLITE3 TRONG PYTHON
    try:
        print("🔧 Đang ghi đè Cookie vào file tạm...")
        # Kết nối tới file tạm vừa được chmod 777
        conn = sqlite3.connect(TEMP_DB)
        cursor = conn.cursor()
        
        # Logic xóa và chèn cookie chuẩn
        cursor.execute("DELETE FROM cookies WHERE host_key = '.roblox.com' AND name = '.ROBLOSECURITY'")
        
        now = int(time.time())
        sql = """INSERT INTO cookies 
                 (creation_utc, host_key, name, value, path, expires_utc, is_secure, is_httponly, last_access_utc, has_expires, is_persistent, priority, samesite, source_scheme) 
                 VALUES (?, '.roblox.com', '.ROBLOSECURITY', ?, '/', 253402300799, 1, 1, ?, 1, 1, 1, -1, 2)"""
        cursor.execute(sql, (now, MY_COOKIE, now))
        
        conn.commit()
        conn.close()
        print("✅ Đã sửa dữ liệu file tạm thành công.")
    except Exception as e:
        print(f"❌ Lỗi ghi SQLite: {e}")
        return

    # BƯỚC 4: PASTE LẠI VÀ FIX QUYỀN SỞ HỮU (IMPORTANT)
    print("📥 Đang nạp dữ liệu vào lại ứng dụng...")
    uid = get_uid()
    run_tsu(f"cp {TEMP_DB} {REAL_DB}")
    run_tsu(f"chown {uid} {REAL_DB}") # Trả file cho app để không bị logout
    run_tsu(f"chmod 660 {REAL_DB}")
    run_tsu(f"rm {TEMP_DB}")

    # BƯỚC 5: KHỞI ĐỘNG GAME (Bê nguyên logic launch_roblox dòng 1018)
    print(f"\033[1;96m[ Rokid Manager ] - Đang tham gia server ID: {PLACE_ID}...\033[0m")
    link = f"roblox://placeID={PLACE_ID}"
    
    # Bước 1: ActivitySplash (Dòng 1024)
    run_tsu(f"am start -n {PACKAGE_NAME}/com.roblox.client.startup.ActivitySplash -d {link}")
    time.sleep(5)

    # Bước 2: ActivityProtocolLaunch (Dòng 1030)
    run_tsu(f"am start -n {PACKAGE_NAME}/com.roblox.client.ActivityProtocolLaunch -d {link}")
    
    print("\033[1;92m[ Rokid Manager ] - Hoàn tất quy trình Rejoin!\033[0m")

if __name__ == "__main__":
    execute_process()
